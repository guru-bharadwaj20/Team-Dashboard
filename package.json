{
  "name": "team-decision-board",
  "private": true,
  "version": "2.0.0",
  "description": "Real-time team decision board with consensus tracking and AI decision summaries",
  "license": "MIT",
  "engines": {
    "node": ">=18.18"
  },
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "start": "npm run start -w backend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "test": "npm run test -w backend",
    "migrate:comments": "npm run migrate:comments -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
